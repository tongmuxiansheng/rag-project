# RAG 知识库系统依赖
# 向量数据库
chromadb>=0.4.0

# 文档处理
pdfplumber>=0.10.0      # PDF 处理（表格提取效果好，推荐）
pypdf>=3.17.0           # PDF 处理（备用）
python-docx>=1.1.0      # Word 处理
openpyxl>=3.1.0         # Excel 处理

# 本地向量化模型
sentence-transformers>=2.2.0

# 大模型 API
openai>=1.0.0

# 后端 API
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# 工具
numpy>=1.24.0
python-dotenv>=1.0.0

# OCR 文字识别（可选，识别 PDF 图片中的文字）
# 安装命令：pip install paddlepaddle paddleocr
# 注意：PaddleOCR 对 Python 版本有要求，如安装失败可使用 easyocr 替代
# paddlepaddle
# paddleocr
